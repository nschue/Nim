<?xml version="1.0" encoding="utf-8"?>
<!-- Main layout for game activity -->
    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/mainRelativeLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:background="@color/boardColor"
        tools:context=".GameActivity">
    <!--End GameActivity Button delcaration -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/end_game_button"
        android:textColor="#ffffff"
        android:gravity="center"
        android:background="@drawable/button"
        android:padding="10dp"
        android:text="End Game"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" />

        <!-- Displays the current player -->
        <TextView
            android:id="@+id/currentPlayerTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_alignParentTop="true"
            android:padding="10dp"
            android:textColor="@color/textColor"
            android:textSize="30dp" />
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/endTurnButton"
            android:orientation="vertical"
            android:id="@+id/gameboard_container">

        </LinearLayout>

        <!-- End turn button declaration -->
        <Button
            android:id="@+id/endTurnButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#ffffff"
            android:background="@drawable/button"
            android:padding="10dp"
            android:text="End Turn"
            android:layout_centerHorizontal="true"
            android:layout_alignParentBottom="true"/>

    </RelativeLayout>
